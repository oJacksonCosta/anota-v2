import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Anota | Login",
};

export default function Login() {
  return (
    <section>
      <h1>Login</h1>
    </section>
  );
}
